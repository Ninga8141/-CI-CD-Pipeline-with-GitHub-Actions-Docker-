echo name: CI-CD Pipeline > ci-cd.yml
echo on: >> ci-cd.yml
echo   push: >> ci-cd.yml
echo     branches: [ "main" ] >> ci-cd.yml
echo jobs: >> ci-cd.yml
echo   build: >> ci-cd.yml
echo     runs-on: ubuntu-latest >> ci-cd.yml
echo     steps: >> ci-cd.yml
echo     - uses: actions/checkout@v4 >> ci-cd.yml
echo     - uses: docker/setup-buildx-action@v3 >> ci-cd.yml
echo     - name: Login to DockerHub >> ci-cd.yml
echo       uses: docker/login-action@v3 >> ci-cd.yml
echo       with: >> ci-cd.yml
echo         username: ${{ secrets.DOCKERHUB_USERNAME }} >> ci-cd.yml
echo         password: ${{ secrets.DOCKERHUB_TOKEN }} >> ci-cd.yml
echo     - name: Build Docker Image >> ci-cd.yml
echo       run: docker build -t yourdockerhubusername/myapp:latest . >> ci-cd.yml
echo     - name: Push Docker Image >> ci-cd.yml
echo       run: docker push yourdockerhubusername/myapp:latest >> ci-cd.yml
